window.onload = function() {
    //These functions will not run until the entire page is loaded


    let commentList = document.getElementById('new-name-comment');
    commentList.addEventListener('submit', retrieveInputs);

}
//Get Axios comment data
function retrieveAxiosData() {

}
//Listens for the onclick event and passes the new comment information to axios
function retrieveInputs(event) {
    event.preventDefault();

    //add a post axios thingy

    function retrieveAxiosData();
}
//Populates the Comment Section with Axios data
function populateCommentSection() {
    
}